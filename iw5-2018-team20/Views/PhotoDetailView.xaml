<Page x:Class="iw5_2018_team20.Views.PhotoDetailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModels="clr-namespace:iw5_2018_team20.Views"
      DataContext="{Binding PhotoDetailViewModel, Source={StaticResource ViewModelLocator}}"
      mc:Ignorable="d" 
      d:DesignHeight="600"
      d:DesignWidth="400">

    <Grid DataContext="{Binding Detail}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Název:"></Label>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}"></TextBox>

        <Label Grid.Row="1" Grid.Column="0" Content="Datum:"></Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CreationTime}"></TextBox>

        <Label Grid.Row="2" Grid.Column="0" Content="Formát:"></Label>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Format}"></TextBox>

        <Label Grid.Row="3" Grid.Column="0" Content="Rozlišení:"></Label>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Dimensions}"></TextBox>


        <Label Grid.Row="4" Grid.Column="0" Content="Poznámka:"></Label>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Note}"></TextBox>

        <Image Grid.Row="5"  Source="{Binding Path}" Grid.ColumnSpan="2"/>

        <Label Grid.Row="6" Grid.Column="0" Content="Objekty na fotografii"></Label>
        <ItemsControl Grid.Row="7" Grid.ColumnSpan="2" ItemsSource="{Binding ObjectsOnPhoto}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{}"></TextBlock>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
